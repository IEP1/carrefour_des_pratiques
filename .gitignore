# Documents sources contenant des données personnelles (noms d'enseignants) :
# ne jamais publier sur le repo de code, qui doit rester public pour GitHub Pages.
*.docx
*.xlsx
desktop.ini
01 Logo IEP1.png

# Import ponctuel des enseignants réels (données personnelles) : ne jamais publier,
# utilisé une seule fois en local pour peupler le repo privé de données.
js/import-data.local.js
import.html

# État local de la CLI Netlify (aucun secret dedans, mais propre à chaque machine)
.netlify/

# Outil de migration à usage unique (GitHub → Supabase), plus utile une fois la migration faite
migrer-vers-supabase.html

# Mots de passe / secrets notés localement : ne jamais publier
*.txt
MDP*
